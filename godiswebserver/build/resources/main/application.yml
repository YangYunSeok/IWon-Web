server:
  port: 8080

---
spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    # DB 접속 정보를 프로젝트 요구사항에 맞게 업데이트했습니다.
    # database name has been set to `menu_tree`. Change it if your schema differs.
    # Update the database name to GODIS as per the latest requirement
    # url: jdbc:mariadb://localhost/GODIS?serverTimezone=Asia/Seoul&useUnicode=true&characterEncoding=utf8
    url: jdbc:mariadb://192.168.0.120:3306/GODIS?serverTimezone=Asia/Seoul&useUnicode=true&characterEncoding=utf8
    username: sbtAdmin
    password: "iteyes123$"
    hikari:
      maximum-pool-size: 10
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher

  kafka:
    bootstrap-servers: 192.168.0.122:9092
    consumer:
      group-id: testGroup

  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=1000,expireAfterWrite=24h

app:
  datasource:
    maria120:
      driver-class-name: org.mariadb.jdbc.Driver
      enabled: true
      jdbc-url: jdbc:mariadb://localhost:3306/GODIS?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
      username: sbtAdmin
      password: "iteyes123$"
      pool-name: hikari-maria-120
      maximum-pool-size: 10
      minimum-idle: 0
      initialization-fail-timeout: 0
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-test-query: "SELECT 1"

# application.yml
logging:
  level:
    com.godisweb.mapper: DEBUG
    org.apache.ibatis: DEBUG
    org.apache.ibatis.logging.jdbc: DEBUG

mybatis:
  # The package containing your domain classes; MyBatis will create short names automatically.
  type-aliases-package: com.godisweb.model
  # We are using annotation‑based SQL in the mapper, so mapper XMLs are optional. Leave this
  # setting in case you add XML mappers later.
  mapper-locations: classpath:mapper/**/*.xml

  # Configure MyBatis to use Log4j2 for logging SQL statements. This
  # implementation will delegate log messages to our Log4j2
  # configuration (log4j2-spring.xml). When set to DEBUG level in
  # Log4j2, executed SQL statements and parameters will be printed to
  # the console.  
  configuration:
    #log-impl: org.apache.ibatis.logging.log4j2.Log4j2Impl
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
    auto-mapping-behavior: FULL          # (권장) 별도 resultMap 없이 최대한 자동 매핑
